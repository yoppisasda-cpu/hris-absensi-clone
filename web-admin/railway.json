{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && ls -la out"
  },
  "deploy": {
    "startCommand": "npx serve -s out -l 3000",
    "restartPolicyType": "ON_FAILURE"
  }
}
