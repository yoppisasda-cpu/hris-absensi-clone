{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && ls -la out"
  },
  "deploy": {
    "startCommand": "npx serve -s out -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
